.DS_Store
Thumbs.db
*.log
node_modules/
.claude/

# Secrets & service-account keys
# Case-robust (Windows core.ignorecase mascara, mas Linux/CI é case-sensitive)
# + nomes típicos das chaves admin do Firebase
[Ss]ervice[Aa]ccount*.json
*firebase-adminsdk*.json
set-admin-claims.js
*.pem
.env
.env.local

# O package.json/package-lock.json da raiz SÃO versionados: declaram o tooling
# de testes (@firebase/rules-unit-testing) que o CI instala para correr os
# testes de regras. (functions/package.json também é versionado, p/ o deploy.)
